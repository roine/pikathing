{
  "name": "plume",
  "version": "1.0.0",
  "description": "When starting an ELM project I always end up writing the same boilerplate and configuring the same packaging system. Elm boilerplate takes care of that for you.",
  "main": "index.js",
  "dependencies": {
    "node-elm-compiler": "^5.0.1",
    "parcel-bundler": "^1.11.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html",
    "build": "parcel build index.html",
    "deploy": "parcel build index.html && cp CNAME dist && cd dist && now --local-config=../now.json && cd .. && now alias && now remove plume --safe -y"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/roine/elm-bootstrap.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/roine/elm-bootstrap/issues"
  },
  "homepage": "https://github.com/roine/elm-bootstrap#readme",
  "devDependencies": {
    "elm-hot": "^1.0.1"
  }
}
